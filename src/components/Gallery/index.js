// Import dependencies
import React from 'react';

// Create the `Gallery` function
const Gallery = () => {
  return (
    <section>
      <h1>NAME</h1>
      <p>DESCRIPTION</p>
    </section>
  );
};

// Export the module
export default Gallery;
